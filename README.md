# Docker PHP HTTP starter
To run the project use docker-compose:

    docker-compose up

Or run it as a background process:

    docker-compose up -d

You can work on PHP related stuff inside an app container:

    docker-compose run app bash
